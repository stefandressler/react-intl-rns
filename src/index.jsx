export {
  rnsShorts,
  RNSMessage
} from './RNSMessage';

export const config = {
  // String used to separate namespace prefixes from message and format ids.
  separator: '.',
  // prefix for defaultMessage output when key not found.
  defaultMessagePrefix: '__intl|'
};
